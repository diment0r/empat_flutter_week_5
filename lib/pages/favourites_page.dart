import 'package:flutter/material.dart';

class SkillsPageWidget extends StatelessWidget {
  const SkillsPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Favourites'),
    );
  }
}
